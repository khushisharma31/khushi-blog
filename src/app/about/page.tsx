import AboutClient from "@/components/AboutClient";

export const metadata = {
  title: "About — Khushi Sharma",
  description: "AI engineer. Writer. Reaching toward the light.",
};

export default function AboutPage() {
  return <AboutClient />;
}
